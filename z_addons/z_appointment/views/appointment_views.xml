<odoo>
  <data>
    <record id="z_appointment_list_view" model="ir.ui.view">
      <field name="name">z_appointment.appointment.list</field>
      <field name="model">z_appointment.appointment</field>
      <field name="arch" type="xml">
        <tree string="Visits" create="0" delete="0" duplicate="0">
          <field name="booking_date"/>
          <field name="booking_time"/>
          <field name="type"/>
          <field name="technician_id"/>
          <field name="doctor_id"/>
          <field name="state"/>
          <field name="note" string="Appointment note"/>
          <field name="customer_note" widget="Note"/>
        </tree>
      </field>
    </record>
    
    <record id="view_appointment" model="ir.actions.act_window">
      <field name="name">Visits</field>
      <field name="res_model">z_appointment.appointment</field>
      <field name="view_mode">tree</field>
      <field name="domain">[('customer_id','=',active_id)]</field>
      <field name="view_id" eval="False"/>
    </record>

    <record model="ir.actions.act_window.view" id="z_appointment_action_view">
      <field name="sequence" eval="2"/>
      <field name="view_mode">tree</field>
      <field name="view_id" ref="z_appointment_list_view"/>
      <field name="act_window_id" ref="view_appointment"/>
    </record>
  </data>
</odoo>
